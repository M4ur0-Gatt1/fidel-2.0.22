requests>=2.31
pywebview>=5.0
PyQt5>=5.15  # Backend Qt para evitar errores .NET en Windows
pythonnet>=3.0
cffi>=1.16
pillow>=10.0
vtracer>=0.6         # raster → SVG editable (vectorizar / "calcar"); offline
# ffmpeg (binario, no pip) habilita export a MP4 y video; opcional
# Para compilar: pip install pyinstaller
