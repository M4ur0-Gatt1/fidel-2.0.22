config.json
__pycache__/
dist/
build/
*.zip
*.backup
.venv/

# salidas que LOW genera cuando se usa el propio repo como workspace
# (no son código fuente; se ignoran para que no se acumulen en el repo).
# Lo ya versionado no se borra: esto aplica de acá en más.
disenos/
video/
social/
assets/
.fidel/
.low/
*.mp4
Output/
